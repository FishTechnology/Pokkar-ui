import { FormatPointPipe } from './format-point.pipe';

describe('FormatPointPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPointPipe();
    expect(pipe).toBeTruthy();
  });
});
